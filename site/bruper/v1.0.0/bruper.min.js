class Bruper {
  constructor(elem, css) {
    this.elem = function() {
      document.createElement(elem);
    }
    this.css = css;
    this.run = function(where) {
      var elem = this.elem();
      elem.style = this.css;
      document.querySelector(where).appendChild(elem)
    }
  }
}
